package com.flowledger.demo.images;

/** Future extension: load thousands of images from a ZIP without code changes to seeders. */
public interface ZipCategoryImageProvider extends CategoryImageProvider {}
